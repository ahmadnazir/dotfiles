# Enable all Magic SysRq functions (task/memory dumps, safe reboot sequence).
# Not stowed automatically (lives outside $HOME) — install once with:
#   sudo cp ~/dotfiles/99-sysrq.conf /etc/sysctl.d/99-sysrq.conf
#   sudo sysctl --system
kernel.sysrq = 1
